---
name: xiaoying-travel-page
description: 根据机票、酒店和每日行程资料生成或修改适合手机查看的旅行网页，包含航班切换、分国分日路线图、门票入口、自驾和旅行记账。适用于整理旅行资料成网页，以及复用瑞士意大利 13 天页面效果。
---

# 小颖旅行网页

以 `assets/template/` 的最终版网页为底板，保留浅色纸张背景、深色航班卡片和手机布局。默认复用已有交互；用户明确要求调整样式或功能时，修改对应文件。

## 生成

1. 提取用户材料里的旅行日期、航段和时区、住宿、每日路线、门票和租车信息。根据已有资料启用航班、地图、每日行程、租车、待办、记账；沿用用户已确认的选择，不强制重复两轮确认。仅询问会影响结果的冲突，其他缺项先写待补充。
2. 在用户指定目录生成一个独立项目，不直接修改本 Skill 模板：

   ```bash
   python3 scripts/create_trip.py /绝对路径/我的旅行
   ```

   以上命令在本 Skill 目录运行。输出目录必须不存在，避免覆盖旧旅行。只有用户要求演示或复用瑞士意大利内容时，加 `--demo`。默认创建空白数据；不要让上一趟旅行的机票和账单混入新旅行。
3. 读取输出项目的 `trip-data.json` 和 [数据指南](references/data-guide.md)，把本次资料填写进去。`metadata.tripId` 使用本次旅行的唯一 ID，避免浏览器存储串用。将 `trip.status` 改成 draft。用户要求虚构演示时可以生成演示数据，但不能把虚构航班、价格、预订状态称为已核实事实。
4. 在输出项目目录执行：

   ```bash
   npm run build:map
   npm run validate
   npm run preview
   ```

   无 npm 时可直接运行对应 Node 脚本。Node 20 或以上，无需安装网页依赖。地图关闭时可跳过构建。保留预览服务运行，读取日志中的实际端口并验证 HTTP 可访问后，给用户可点击地址。本地地址仅在服务运行的设备上可用。

## 最终版交互要求

- 航班多于一组时，保留可点击的上一班、下一班和圆点；手机滑动也能切换。使用各机场日期和 UTC 偏移计算转机时间、出发倒计时，不能只比较钟表时间。
- 地图按目的地国家拆分，国家和日期标签必须能点击。只展示当天路线，保持底图和地点位置稳定。地图是路线示意，保留地图说明；实际导航使用地点链接。转机国家不自动加入旅行地图。
- 行程可以展开，住宿写进当天入住/退房或住宿行程。门票“查看”展示官方入口或用户提供的 PDF。官方网址须通过景点官网核实；不伪造购票链接、订单或票据。
- 自驾展示租车公司、车型、取还车时间地点、费用及用户提供的提醒。资料缺失可以留空，不能猜成已预订。
- 记账保留付款人、参与分摊人、收支和结算算法。只在新账本为空时使用 initialLedger；改网页不能覆盖浏览器已有账单。修改相关代码时运行 `npm run test:runtime`。
- 待办从用户资料提取；额外建议只在用户要求时添加。

## 验证和交付

用浏览器检查启用的交互，至少确认窄屏下航班第二张可见、国家/日期切换、行程展开、门票对话框和记账入口。出现问题修复对应模块，不重写整个页面。缺少浏览器能力时，说明交互未实测，不能把静态校验称为点击验证。

默认本地保存记账、待办和门票状态。部署成网址只会共享静态页面，不自动同步不同手机的账单；多人同步需要另做用户自己的共享存储。不要把运行时存储、私人订单 PDF、身份或预订凭据上传到公共 Skill 仓库。发布个人旅行页面时检查实际授权范围，按用户要求处理私人材料。

如果用户请求 GitHub 或静态托管部署，继续完成已授权的发布工作；只有真实可访问并验证过的地址才能称为上线地址。

## 来源

基于 do 同学的 [Travel-Plan-Page](https://github.com/do-tongxue/Travel-Plan-Page) 二次整理，包含本次完成的航班控制、地图交互、门票入口和记账种子功能。保留 [MIT 许可证](LICENSE) 与 [第三方说明](THIRD_PARTY_NOTICES.md)，不要宣称整个框架为独立原创。
